<a href="{{ this.url }}" hreflang="{{ this.targetLocale }}" lang="{{ this.targetLocale }}" class="language-switch">
  {{ this.targetLocale }}
  <span class="sr-only">— {{ 'language.switch'|trans }}</span>
</a>
